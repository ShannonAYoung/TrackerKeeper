export enum Platform {
  UNKNOWN = "UNKNOWN",
  IOS = "iOS",
  ANDROID = "Android"
}

export enum ConnectionStatus {
  DISCONNECTED = "DISCONNECTED",
  SEARCHING = "SEARCHING",
  CONNECTED = "CONNECTED"
}